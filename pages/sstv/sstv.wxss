.container {
  min-height: 100vh;
  background: var(--bg-primary);
  padding-bottom: 40rpx;
}

.hidden-canvas {
  width: 320rpx;
  height: 240rpx;
  position: absolute;
  top: -1000rpx;
  left: -1000rpx;
}

/* 标签栏 */
.tab-bar {
  display: flex;
  background: var(--card-bg);
  padding: 20rpx 0;
  margin-bottom: 20rpx;
  box-shadow: 0 2rpx 10rpx var(--shadow-color);
}

.tab-item {
  flex: 1;
  text-align: center;
  padding: 20rpx;
  font-size: 30rpx;
  color: var(--text-secondary);
  border-bottom: 4rpx solid transparent;
  transition: all 0.3s;
}

.tab-item.active {
  color: var(--accent-primary);
  border-bottom-color: var(--accent-primary);
  font-weight: 600;
}

/* 上传区域 */
.upload-area {
  background: var(--card-bg);
  border-radius: 20rpx;
  padding: 60rpx 40rpx;
  text-align: center;
  margin: 20rpx;
  border: 4rpx dashed var(--border-color);
  transition: all 0.3s;
  position: relative;
  overflow: hidden;
  min-height: 300rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.upload-area:active {
  opacity: 0.8;
  border-color: var(--accent-primary);
}

.upload-area.has-image {
  padding:0;
  border-style: solid;
  border-color: var(--border-color);
  overflow: visible;
}

.upload-icon {
  font-size: 80rpx;
  margin-bottom: 20rpx;
}

.upload-text {
  display: block;
  font-size: 32rpx;
  color: var(--text-primary);
  margin-bottom: 10rpx;
}

.upload-hint {
  font-size: 24rpx;
  color: var(--text-secondary);
}

.upload-preview {
  width: 100%;
  height: 400rpx;
  border-radius: 16rpx;
  display: block;
}

.upload-actions {
  position: absolute;
  top: 20rpx;
  right: 20rpx;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  z-index: 10;
}

.action-btn-small {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 12rpx 16rpx;
  border-radius: 16rpx;
  background: rgba(0, 0, 0, 0.65);
  backdrop-filter: blur(20rpx);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.3);
  border: 2rpx solid rgba(255, 255, 255, 0.2);
}

.action-btn-small:active {
  transform: scale(0.92);
  background: rgba(0, 0, 0, 0.85);
  border-color: rgba(255, 255, 255, 0.4);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.5);
}

.action-btn-small.reselect:active {
  background: rgba(59, 130, 246, 0.8);
  border-color: rgba(59, 130, 246, 0.6);
}

.action-btn-small.remove:active {
  background: rgba(220, 38, 38, 0.9);
  border-color: rgba(220, 38, 38, 0.7);
}

.action-icon {
  font-size: 36rpx;
  line-height: 1;
  margin-bottom: 4rpx;
}

.action-label {
  font-size: 20rpx;
  color: rgba(255, 255, 255, 0.95);
  font-weight: 500;
  letter-spacing: 2rpx;
  text-shadow: 0 1rpx 2rpx rgba(0, 0, 0, 0.5);
}

.action-btn-small.reselect {
  background: rgba(59, 130, 246, 0.75);
  border-color: rgba(59, 130, 246, 0.5);
}

.action-btn-small.reselect .action-label {
  color: #FFFFFF;
}

.action-btn-small.remove {
  background: rgba(220, 38, 38, 0.75);
  border-color: rgba(220, 38, 38, 0.5);
}

.action-btn-small.remove .action-label {
  color: #FFFFFF;
}

.image-size-label {
  position: absolute;
  bottom: 20rpx;
  left: 50%;
  transform: translateX(-50%);
  background: rgba(0, 0, 0, 0.6);
  color: #FFFFFF;
  font-size: 22rpx;
  padding: 8rpx 20rpx;
  border-radius: 20rpx;
  backdrop-filter: blur(10rpx);
}

/* 按钮 */
.control-group {
  padding: 0 20rpx;
}

.action-btn {
  width: 100%;
  height: 96rpx;
  line-height: 96rpx;
  border-radius: 999rpx;
  font-size: 32rpx;
  font-weight: 600;
  border: none;
  margin-bottom: 20rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.action-btn.primary {
  background: linear-gradient(135deg, var(--accent-primary), #A85540);
  color: var(--text-inverse);
}

.action-btn.danger {
  background: linear-gradient(135deg, var(--error), #A55555);
  color: var(--text-inverse);
}

.action-btn.secondary {
  background: var(--bg-primary);
  color: var(--text-primary);
}

.action-btn:active {
  opacity: 0.9;
}

.action-btn[disabled] {
  background: var(--border-color);
  color: var(--text-placeholder);
}

/* 设置区域 */
.encode-settings, .decode-settings {
  padding: 20rpx;
  background: var(--card-bg);
  margin: 20rpx;
  border-radius: 16rpx;
}

.setting-item {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.setting-label {
  font-size: 28rpx;
  color: var(--text-primary);
  width: 140rpx;
}

.setting-slider {
  flex: 1;
}

.setting-value {
  font-size: 26rpx;
  color: var(--accent-primary);
  width: 80rpx;
  text-align: right;
}

/* 解码状态 */
.decode-status {
  text-align: center;
  padding: 40rpx 20rpx;
}

.status-circle {
  width: 160rpx;
  height: 160rpx;
  border-radius: 50%;
  background: var(--color-log);
  margin: 0 auto 20rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s;
}

.status-circle.active {
  background: linear-gradient(135deg, var(--accent-active), #6B9A5F);
  animation: pulse 1s infinite;
}

@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

.status-icon {
  font-size: 60rpx;
}

.status-text {
  display: block;
  font-size: 32rpx;
  color: var(--text-primary);
  margin-bottom: 10rpx;
}

.status-hint {
  font-size: 24rpx;
  color: var(--text-secondary);
}

.decode-controls {
  padding: 0 20rpx;
}

/* 解码预览 */
.decode-preview {
  background: var(--card-bg);
  margin: 20rpx;
  padding: 20rpx;
  border-radius: 16rpx;
  text-align: center;
}

.preview-header {
  margin-bottom: 15rpx;
}

.preview-title {
  font-size: 26rpx;
  color: var(--text-primary);
  font-weight: 600;
}

.preview-canvas {
  border-radius: 12rpx;
  background: var(--text-primary);
  display: block;
  margin: 0 auto;
}

.preview-info {
  margin-top: 15rpx;
  font-size: 24rpx;
  color: var(--text-secondary);
}

/* 解码进度 */
.decode-progress {
  padding: 20rpx;
  background: var(--card-bg);
  margin: 20rpx;
  border-radius: 16rpx;
}

.progress-bar {
  height: 20rpx;
  background: var(--bg-primary);
  border-radius: 10rpx;
  overflow: hidden;
  margin-bottom: 10rpx;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--accent-active), var(--accent-secondary));
  transition: width 0.3s;
}

.progress-text {
  font-size: 26rpx;
  color: var(--text-secondary);
  text-align: center;
}

/* 结果图片 */
.result-image-container {
  padding: 20rpx;
  background: var(--card-bg);
  margin: 20rpx;
  border-radius: 16rpx;
}

.result-label {
  display: block;
  font-size: 28rpx;
  color: var(--text-primary);
  margin-bottom: 15rpx;
}

.result-image {
  width: 100%;
  max-height: 400rpx;
  border-radius: 12rpx;
  margin-bottom: 20rpx;
}

.result-actions {
  display: flex;
  gap: 20rpx;
}

.result-actions .action-btn {
  flex: 1;
}

/* 提示 */
.tips {
  padding: 20rpx;
  background: rgba(107, 142, 174, 0.1);
  margin: 20rpx;
  border-radius: 16rpx;
  border-left: 6rpx solid var(--info);
}

.tips-title {
  display: block;
  font-size: 28rpx;
  color: var(--info);
  font-weight: 600;
  margin-bottom: 15rpx;
}

.tips-content text {
  display: block;
  font-size: 26rpx;
  color: var(--text-secondary);
  line-height: 1.8;
}

/* 新音频面板样式 */
.audio-panel {
  background: var(--card-bg);
  margin: 20rpx;
  border-radius: 20rpx;
  padding: 30rpx;
  box-shadow: 0 4rpx 20rpx var(--shadow-color);
}

.audio-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24rpx;
}

.audio-header-right {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.audio-download-btn {
  display: flex;
  align-items: center;
  gap: 6rpx;
  padding: 8rpx 20rpx;
  border-radius: 20rpx;
  background: rgba(59, 130, 246, 0.15);
  border: 2rpx solid rgba(59, 130, 246, 0.3);
  transition: all 0.3s;
}

.audio-download-btn:active {
  background: rgba(59, 130, 246, 0.3);
  transform: scale(0.95);
  border-color: rgba(59, 130, 246, 0.6);
}

.download-icon {
  font-size: 28rpx;
  line-height: 1;
}

.download-text {
  font-size: 22rpx;
  color: #3B82F6;
  font-weight: 500;
}

.audio-title {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.audio-format {
  font-size: 22rpx;
  color: var(--text-secondary);
  background: var(--bg-primary);
  padding: 6rpx 16rpx;
  border-radius: 8rpx;
}

.audio-progress-container {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-bottom: 24rpx;
}

.audio-time {
  font-size: 22rpx;
  color: var(--text-secondary);
  font-family: ui-monospace, SFMono-Regular, monospace;
  min-width: 60rpx;
  text-align: center;
}

.audio-progress-bar {
  flex: 1;
  height: 8rpx;
  background: var(--bg-primary);
  border-radius: 4rpx;
  overflow: hidden;
}

.audio-progress-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--accent-primary), #A85540);
  border-radius: 4rpx;
  transition: width 0.3s;
}

.audio-controls {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.audio-play-btn {
  width: 96rpx !important;
  height: 96rpx !important;
  border-radius: 50% !important;
  background: linear-gradient(135deg, var(--accent-primary), #A85540) !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  border: none !important;
  padding: 0 !important;
  line-height: 1 !important;
  margin: 0 !important;
  min-height: 96rpx !important;
}

.audio-play-btn.playing {
  background: linear-gradient(135deg, var(--error), #A55555) !important;
}

.audio-play-btn::after {
  border: none !important;
  display: none !important;
}

.play-icon {
  font-size: 36rpx;
  color: #FFFFFF;
}

.audio-info-tags {
  display: flex;
  gap: 12rpx;
  flex-wrap: wrap;
}

.info-tag {
  font-size: 20rpx;
  color: var(--accent-primary);
  background: rgba(196, 100, 62, 0.1);
  padding: 8rpx 16rpx;
  border-radius: 8rpx;
  font-weight: 500;
}

/* 图片容器 - 相对定位用于呼号绝对定位 */
.image-container {
  position: relative;
  width: 320px;
  height: 240px;
  overflow: visible;
  margin: 0 auto;  /* 居中显示 */
}

/* 呼号覆盖层 - 可拖动 */
.callsign-overlay {
  position: absolute;
  z-index: 999;
  padding: 8rpx 16rpx;
  background: rgba(0, 0, 0, 0.7);
  border-radius: 8rpx;
  display: none;
  cursor: move;
  user-select: none;
  pointer-events: auto;
  /* 位置由 wxml 中的 style 动态控制，不要在这里固定 */
  top: auto;
  right: auto;
}

.callsign-overlay.show {
  display: block !important;
}

.callsign-text {
  font-size: 32rpx;
  font-weight: bold;
  color: #FFFFFF;
  text-shadow: 2rpx 2rpx 4rpx rgba(0, 0, 0, 0.8);
  font-family: monospace;
  white-space: nowrap;
}



/* 呼号按钮样式 */
.callsign-btn {
  background: rgba(196, 100, 62, 0.1) !important;
  border: 2rpx solid var(--accent-primary) !important;
}

.callsign-btn .action-icon,
.callsign-btn .action-label {
  color: var(--accent-primary) !important;
}
